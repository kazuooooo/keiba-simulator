= javascript_include_tag("cocoon")
- case @mode
- when "try"
  h3 検証モードは指定した条件で馬券を購入し、その条件で購入すれば過去のレースにおいて勝てるのか検証できます。
- when "analyze"
  h3 分析モードは指定したオッズの範囲を0.1ずつ全て購入し、どのオッズが勝ちやすいかを分析できます。
= render partial: "shared/betcondition_form", locals: { bet_condition: @bet_condition, mode: @mode }
